const URLs = {
  // Player API's
  GET_PLAYERS_LIST: 'users/win-ratio',

  // Matches API's
  GET_MATCHES_LIST: 'users/match-stats',

  // Add new match API's
  ADD_NEW_MATCH_URL: 'matches'
}

export default URLs
